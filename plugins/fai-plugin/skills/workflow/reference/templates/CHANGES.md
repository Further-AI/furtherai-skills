# Change Log — <Workflow Name>

Build log for this workflow directory. Newest date first; one `## YYYY-MM-DD` section per date, one `- ` bullet per applied change. The builder appends an entry after each build stage; the orchestrator records upload, test, and publish events. Keep entries to one line: what changed + why (+ ticket/design ref).

## YYYY-MM-DD

- <what changed + why> (<ticket, design version, or "—">)
