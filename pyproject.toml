[project]
name = "furtherai-skills"
version = "0.1.0"
description = "FurtherAI-authored agent skills"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pydantic>=2,<3", "pyyaml>=6,<7", "pytest>=8", "ty"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
